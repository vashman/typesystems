//

#include "../../include/typesystem.hpp"

using typesystems::typesystem;

int main() try {

/* create a empty typesystem with no types. */
typesystem typesys;

return 0;
} catch (...) {
return 1;
}
